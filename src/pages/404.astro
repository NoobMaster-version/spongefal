---
import Base from '../layouts/Base.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<Base title="404 — signal lost" description="Page not found.">
  <div class="container lost">
    <p class="label">ERR · localization failure</p>
    <h1>Signal lost</h1>
    <p class="prose">
      This page doesn't exist. Maybe it never did, maybe it moved.
    </p>
    <p class="lost-route label" aria-hidden="true">
      [you] ─┐<br />
      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└──→ replanning… ──→ [home]
    </p>
    <div class="lost-actions">
      <a class="btn btn--primary" href={`${base}/`}>go home</a>
      <a class="btn" href={`${base}/projects/`}>see the projects</a>
    </div>
  </div>

  <style>
    .lost { padding-top: var(--sp-24); }
    .lost-route {
      color: var(--warn);
      margin-block: var(--sp-6);
      line-height: 1.8;
    }
    .lost-actions { display: flex; gap: var(--sp-3); flex-wrap: wrap; }
  </style>
</Base>
